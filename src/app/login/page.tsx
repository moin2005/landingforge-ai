"use client";
import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowUpRight, Check, LockKeyhole } from "lucide-react";
import { Button, Input, Label } from "@/components/ui";
export default function LoginPage() { const [signup, setSignup] = useState(false); const router = useRouter(); return <main className="grid min-h-screen lg:grid-cols-[.9fr_1.1fr]"><section className="hidden flex-col justify-between bg-[var(--ink)] p-10 text-white lg:flex"><Link href="/" className="flex items-center gap-2.5 font-bold tracking-[-.03em]"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[var(--coral)] text-sm">L</span>LandingForge<span className="text-[var(--coral)]">.</span></Link><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#b7e4c8]">The local advantage</p><h1 className="display mt-5 max-w-lg text-6xl leading-[.98] tracking-[-.05em]">A better page is a better <span className="text-[var(--sun)]">first impression.</span></h1><div className="mt-9 space-y-3 text-sm text-white/65"><p className="flex items-center gap-2"><Check size={15} className="text-[#b7e4c8]" />Pages built around local intent</p><p className="flex items-center gap-2"><Check size={15} className="text-[#b7e4c8]" />No copywriting degree required</p><p className="flex items-center gap-2"><Check size={15} className="text-[#b7e4c8]" />A first draft in under ten minutes</p></div></div><p className="text-xs text-white/40">© 2025 LandingForge AI</p></section><section className="flex items-center justify-center px-5 py-12"><div className="w-full max-w-[420px]"><div className="mb-10 lg:hidden"><Link href="/" className="flex items-center gap-2.5 font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[var(--coral)] text-sm text-white">L</span>LandingForge<span className="text-[var(--coral)]">.</span></Link></div><div className="mb-8"><h2 className="display text-4xl tracking-[-.04em]">{signup ? "Start building better." : "Welcome back."}</h2><p className="mt-2 text-sm text-[var(--muted)]">{signup ? "Create your workspace and ship your first page." : "Pick up where you left off."}</p></div><button className="flex h-11 w-full cursor-pointer items-center justify-center gap-2 rounded-xl border border-[var(--line)] bg-white text-sm font-semibold hover:border-[var(--ink)]"><span className="font-bold">G</span>Continue with Google</button><div className="my-6 flex items-center gap-3 text-[10px] font-bold uppercase tracking-[.1em] text-[#a6b0aa]"><span className="h-px flex-1 bg-[var(--line)]" />or continue with email<span className="h-px flex-1 bg-[var(--line)]" /></div><form onSubmit={(event) => { event.preventDefault(); router.push("/dashboard"); }} className="space-y-4">{signup && <div><Label htmlFor="name">Full name</Label><Input id="name" placeholder="Jamie Dawson" /></div>}<div><Label htmlFor="email">Work email</Label><Input id="email" type="email" placeholder="you@company.com" required /></div><div><div className="flex items-center justify-between"><Label htmlFor="password">Password</Label>{!signup && <a href="#" className="text-xs font-semibold text-[var(--coral)]">Forgot password?</a>}</div><Input id="password" type="password" placeholder="••••••••" required /></div><Button type="submit" className="h-12 w-full">{signup ? "Create workspace" : "Sign in"}<ArrowUpRight size={16} /></Button></form><div className="mt-7 flex items-center justify-center gap-2 text-xs text-[var(--muted)]"><LockKeyhole size={13} />Your data is encrypted and private.</div><p className="mt-8 text-center text-sm text-[var(--muted)]">{signup ? "Already have an account?" : "New to LandingForge?"} <button onClick={() => setSignup(!signup)} className="cursor-pointer font-bold text-[var(--coral)] hover:underline">{signup ? "Sign in" : "Create an account"}</button></p></div></section></main>; }